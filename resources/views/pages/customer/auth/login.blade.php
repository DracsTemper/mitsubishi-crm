@extends('layouts.customer-auth') @section('title','Customer Sign In') @section('visual-title','Your DHS Motors journey continues.') @section('form')<div class="cx-auth-card"><div class="cx-eyebrow">WELCOME BACK</div><h2>Sign in to My DHS Motors</h2><p>Access your vehicles, bookings and conversations with your dealer.</p><form action="{{ route('login.submit') }}" method="POST">@csrf<div class="cx-field"><label for="customerEmail">EMAIL ADDRESS</label><input id="customerEmail" name="email" type="email" value="{{ old('email') }}" autocomplete="email" required autofocus>@error('email')<div class="invalid-feedback d-block">{{ $message }}</div>@enderror</div><div class="cx-field" style="margin-top:15px"><label for="customerPassword">PASSWORD</label><input id="customerPassword" name="password" type="password" autocomplete="current-password" required>@error('password')<div class="invalid-feedback d-block">{{ $message }}</div>@enderror</div><div class="cx-check"><label><input name="remember" type="checkbox" value="1" @checked(old('remember'))> Remember me</label><a href="{{ route('customer.forgot') }}">Forgot password?</a></div><button class="cx-btn red" type="submit">Sign In <i class="bi bi-arrow-right"></i></button></form><div class="cx-auth-link">New to DHS Motors? <a href="{{ route('customer.register') }}">Create Account</a></div></div>@endsection
