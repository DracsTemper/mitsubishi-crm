<div class="stat-card" style="--accent:{{ $accent??'var(--red)' }}">
    <div class="stat-label">{{ $label }}</div>
    <div class="stat-value">{{ $value }}</div>
    <div class="stat-change">{{ $change??'↑ 8.2% from last month' }}</div>
</div>